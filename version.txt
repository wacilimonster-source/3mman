{
  "versionCode": 128,
  "versionName": "1.0.95",
  "updateMessage": "M95 全量修复追踪（M94~M101，其他agent改动，已隔离构建+单测15/15通过）：A 凭据安全（PasswordVault Keystore AES/GCM 加密+旧数据惰性迁移+登出清密码/autoLogin；OCR Bitmap 释放竞态 AtomicReference+在途计数；OCR 训练数据 SHA-256 指纹校验）；B 网络解析（OkHttp 显式超时；DIAG 日志 DEBUG 门控；porny 广告过滤改 host 判定+m3u8/mp4 白名单；decodeVideoUrl 非贪婪正则+判空；裸 Jsoup.connect 改走带 Cookie/代理的 NetworkClientHolder；Cookie 黑名单 name() 精确匹配；ParseProxy 行校验；RetryWhenProcess off-by-one；testPornyAddress 失败回滚；ApiException JsonSyntaxException 修正；死重载删除）；C 播放域（Exo 自愈死循环 lastHealUrl 去重；onConfigurationChanged 常量回归修复；对话框 WindowLeaked 兜底；saveVideoUrl 异步化；直链日志脱敏 maskUrl；JZ 全屏克隆实例 retryAction/全屏钮透传；releaseAllVideos 300ms 节流缺口补 pause；Exo 左半屏 2x 改真·长按语义(与亮度手势互斥)；暂停态变速防护；Handler detach 清理；控件库监听覆盖恢复；IpInputEditText 255）；D 主框架下载（HistoryPresenter onError 补齐；列表刷新丢弃复位+页码 off-by-one；评论区失败转圈不复位；收藏 totalPage 判空；About 双 dialog 卡死；退出去 killProcess；我的页登出入口动态补建；广播注销 context 判空；删除任务异步化+DELETING_IDS 过滤幽灵行；HLS 替换任务落终态+TaskState 快照并发收敛；HLS 分片真并发(3路)+断点续传；DownloadDiag 线程安全+LRU；四 Adapter 图片 tag 防抖；下载服务进度节流+空闲 stopSelf）；E 推荐子系统（画像三表 ConcurrentHashMap；params/scorer volatile+条件 markDirty；RecoStore 快照锁外写盘+persistAsync 合并；方向探测并行化；servedKeys LRU；resolveNow 移出主线程查库+10min 负缓存；离页观看比例结算顺序；回收无条件取消挂起手势；stableId 映射表；引擎初始化 context 判空；noMore 区分错误/耗尽；设置页引擎后台初始化；删除死代码 RecommendFeedActivity(+Manifest/Dagger)）；F 数据横切（saveVideoResult unique→list 兜底；detachAll 统一 7 处读路径；repairMisflagged 一次性标记+只写实改行；initCategory 等长守卫+种子异常记日志；HostJsScope 死类删除；SDCardUtils 惰性获取；ServiceModule 空壳删除；文件名 sanitize；PlaybackEngine default 兜底；正则笔误；SecureRandom；外部缓存判空回退；UpdateDownloadService 死服务删除(+Manifest)；常量拼写修正）；H 搜索作者（双搜索 Presenter 旧订阅 dispose 结果串位根治+totalPage 判空；SearchPornyActivity 筛选持久化对齐 Fragment+页码钳制；筛选重置延迟到应用才提交；AuthorPresenter 自愈写库异步化；代理测试态跨输入失效+保存前自动重测；设置目录移动失败回滚；下载检查异步化；testBaseUrl 按源隔离）。集成：greenDAO 插件兼容（int→long 回退，schemaVersion 保持 12），DownloadVideoService byte 装箱，新增 06_unit_test.py 单测设施。未做项：dislike 硬屏蔽（按指示排除）。",
  "apkDownloadUrl": "https://raw.githubusercontent.com/wacilimonster-source/3mman/master/3mman_v1.0.95.apk",
  "sha256": "6204752163c5a6ea29977fa651f2a6a96de683c50e4ec9f7acbf86a1e3da6824"
}
