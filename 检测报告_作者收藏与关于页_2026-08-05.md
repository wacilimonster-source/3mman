# 3mman App 检测报告（作者收藏 + 关于页 + 播放页收藏按钮 + 图标）

> 生成时间：2026-08-05
> 范围：本轮改动的模块（作者收藏功能、关于页精简、播放页作者收藏按钮、App 图标替换）
> 说明：**未修改任何代码**，仅做静态审查 + 与已验证可用的参考实现（分分钟收藏）比对。

---

## 一、确认存在缺陷（按严重度排序）

### 🔴 [高] 作者收藏列表「左滑删除」失效
- **位置**：`AuthorFavoriteAdapter.java`（构造方法）
- **现象**：在收藏列表左滑点「删除」无反应；只有「长按删除」可用。
- **原因**：`BaseQuickAdapter` 的 `setOnItemChildClickListener` 只对通过 `addOnClickListener(id)` 注册过的子 View 生效。参考实现 `FavoriteAdapter.java:36`、`HistoryAdapter.java:36`、`DownloadVideoAdapter.java:74` 都在构造里调用了 `helper.addOnClickListener(R.id.right_menu_delete)`，而 `AuthorFavoriteAdapter` 漏了这一行。
- **建议**：在 `AuthorFavoriteAdapter` 构造中加入
  ```java
  addOnClickListener(R.id.right_menu_delete);
  ```

### 🟠 [中] 视频源(mman9)作者收藏显示为数字 uid，而非作者名
- **位置**：`BasePlayVideo.onFavoriteAuthorClick()` 调 `presenter.addAuthorFavorite(authorKey, authorKey, source)`；`AuthorActivity.toggleFavorite()` 调 `addAuthorFavorite(uid, uid, source)`
- **现象**：91porny 因 authorKey 即作者名，显示正常；mman9 收藏列表与 `AuthorActivity` 标题显示的是数字 uid。
- **原因**：把 `ownerId` 同时当 `authorName` 传入，没有用真正的展示名。播放页已有 `videoResult.getOwnerName()` 可用。
- **建议**：播放页改为 `addAuthorFavorite(authorKey, videoResult.getOwnerName(), source)`；`AuthorActivity` 跳转时额外带 `authorName`（或从 `ownerId` 查展示名）。

### 🟠 [中] 数据库升级会清空 `AutoCompleteEntity` 表数据
- **位置**：`MySQLiteOpenHelper.onUpgrade()` 的 `MigrationHelper.migrate(...)`，只列出 4 个 DAO
- **现象**：`onUpgrade` 用 `DaoMaster.dropAllTables/createAllTables` 重建全部表，但 MigrationHelper 只为**传入列表**的表回拷数据。
- **原因**：项目共有 5 个 `@Entity`（V9MmanItem / VideoResult / Category / AuthorFavorite / **AutoCompleteEntity**），迁移列表漏了 `AutoCompleteEntityDao`。升级到 v7 时该表数据被清空。
- **建议**：把 `AutoCompleteEntityDao.class` 也加入迁移列表；或确认该表为可丢弃的缓存（搜索联想词），则影响不大。

### 🟠 [中] 播放页作者收藏按钮存在「重复收藏 / 竞态」风险
- **位置**：`BasePlayVideo.isAuthorFavorited`（实例字段）+ `onFavoriteAuthorClick()`
- **现象**：进入播放页到 `refreshAuthorFavoriteState()` 异步完成前，按钮默认 ☆；此时点击会按"未收藏"执行 `add`。
- **原因**：`(authorKey, source)` 唯一性只在应用层保证（DB 无唯一约束，因 GreenDAO 同名 `@Index` 会崩溃），而点击时未实时查重。快速点击可能插入重复行。
- **建议**：点击时直接 `presenter.isAuthorFavorited(authorKey, source)` 实时判断，或在 `addAuthorFavorite` 内先按业务键查重再存。

### 🟡 [低/设计] 无法直接进入作者视频列表页浏览未收藏作者
- **位置**：`AuthorActivity` 仅由 `AuthorFavoriteActivity`（我的→作者收藏→点作者）进入
- **现象**：播放页"作者"tab 内点视频是 `setV9MmanItems + initData` 原地重载，不跳 `AuthorActivity`；想看某作者全部视频必须先收藏。
- **说明**：符合你确认的"点击已收藏作者进视频列表页"设计，但体验略绕。若希望播放页作者 tab 也能进 `AuthorActivity`，可在 `AuthorFragment` 的 item 点击里改跳 `AuthorActivity`。

### 🟡 [低] 播放页作者 tab 复用实例时翻页状态未重置
- **位置**：`AuthorFragment`（BasePlayVideo 内复用实例） + `AuthorPresenter.page`
- **现象**：在作者 tab 内点视频触发 `loadAuthorVideos(false)`，`AuthorPresenter.page` 仅在 `pullToRefresh` 时归 1。若已翻到第 N 页，切到另一个 ownerId 的相关视频会用旧 page 加载，可能拿到错误/空页。
- **建议**：进入新作者时把 `page` 重置为 1（或在 `setV9MmanItem` 中重置 presenter 翻页状态）。`AuthorActivity` 每次新建实例，故无此问题。

### 🟡 [低] 数据库无 `(authorKey, source)` 唯一约束
- **位置**：`AuthorFavorite` 实体
- **原因**：前文为修复启动崩溃，移除了同名 `@Index`（GreenDAO 会生成两条同名唯一索引导致 SQLite 报"重复索引名"崩溃），唯一性改由应用层保证。
- **现状**：正常切换不会重复；仅 D 所述的竞态或未来多入口并发写才可能产生重复行。属已知权衡，非阻塞。

### ⚪ [信息] `AuthorFragment` 存在误导性死导入
- **位置**：`AuthorFragment.java:5` `import android.app.Fragment;`
- **说明**：未被使用（实际继承 `MvpFragment`，由项目统一决定 Fragment 类型），`onAttach(Activity)` 用的是框架签名。不影响运行（已 BUILD SUCCESSFUL），仅代码整洁度问题。

---

## 二、确认正常 / 已正确实现的部分

- ✅ **播放页收藏按钮位置**：`activity_base_play_video.xml` 的 `tv_favorite_author` 已正确置于作者名右侧，可点击、有 `☆/★` 文案切换。
- ✅ **收藏状态刷新**：视频解析完成（`setToolBarLayoutInfo`）会异步读取并刷新收藏态，切换视频也会重新读取。
- ✅ **关于页精简**：已删除"赞赏"入口及弹窗逻辑、GitHub 项目地址 / 主页两项；版本号 `versionName "1.0"`，关于页 `QMUIPackageHelper.getAppVersion()` 显示 `v1.0`；版权改为 `© 3mman`。
- ✅ **更新地址**：`AppApiHelper` 三个 raw URL 已指向 `wacilimonster-source/3mman`。
- ✅ **图标替换**：`ic_launcher` / `ic_launcher_round` 各密度已覆盖；关于页引用 `@mipmap/ic_launcher` 自动同步。
- ✅ **入口**：`MineFragment` 已加"作者收藏"项并跳转 `AuthorFavoriteActivity`。
- ✅ **`fragment_author.xml`** 已无残留的浮动收藏按钮视图，与 `AuthorFragment` 当前逻辑一致。
- ✅ **字符串资源**：`author_favorite` / `author_favorite_on` / `author_favorite_off` 均存在。
- ✅ **编译**：当前代码 `BUILD SUCCESSFUL`（`3mman_v1.0_debug.apk`）。

---

## 三、部署前置问题（非代码缺陷，但会影响功能）

### ⚠️ 检查更新 / 常见问题当前必失败
- **位置**：`AppApiHelper` 的 `version.txt` / `notice.txt` / `COMMON_QUESTION.md` 三个 raw URL
- **原因**：仓库 `git@github.com:wacilimonster-source/3mman.git` 尚未创建/推送成功（SSH key 未添加，push 被拒）。因此这三个文件不存在，点「检查更新」「常见问题」会返回错误。
- **影响**：更新检查、公告、常见问题三项功能在仓库就绪前均不可用。
- **解决**：① 把本机生成的 SSH 公钥加入你的 GitHub；② 在仓库放好 `version.txt`（含 versionCode/versionName）、`notice.txt`、`COMMON_QUESTION.md`；③ 完成后我再 push。

---

## 四、修复优先级建议

| 优先级 | 项 | 工作量 |
|---|---|---|
| P0 | A 滑动删除失效 | 1 行 |
| P1 | B 作者名显示为 uid | 小 |
| P1 | C 迁移漏 AutoCompleteEntity | 1 行 |
| P2 | D 重复收藏竞态 | 小 |
| P3 | E/F/G/H 设计/健壮性 | 视需要 |
| 部署 | 检查更新/常见问题 | 依赖仓库配置 |

> 报告结束。以上均未改动代码；确认后我可按 P0→P1 顺序修复。
