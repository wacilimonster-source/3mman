# 3mman 完全代码审查报告

> 审查日期：2026-08-07 ｜ 审查范围：`app/src\main\java` 全部 Java 源码 + `AndroidManifest.xml` + `res/xml` 网络安全/FileProvider 配置 + `app/build.gradle`
> 审查方式：4 个并行模块深度走查（崩溃/生命周期、数据层/GreenDAO、网络/安全/更新、播放/存储/权限）+ 全局 grep 模式扫描。**全程只读，未修改任何文件。**
> 说明：本报告只"整理问题"，不修复。每个问题标注 `文件:行号`、严重度（高/中/低）、原因与后果。

---

## 一、总览

| 维度 | 高 | 中 | 低 | 备注 |
|---|---|---|---|---|
| 确定性崩溃 / NPE | 13 | 5 | 3 | 含"当前设备即崩"与"旋转后崩" |
| 功能失效 | 1 | 1 | 0 | 左滑删除失效 |
| 数据不可逆丢失 | 4 | 4 | 1 | 升级清空 / 竞态重复 / 误删 |
| 安全 / 供应链 | 2 | 8 | 2 | 更新链路可被投毒 |
| 存储 / 权限 / 兼容性（潜伏） | 4 | 8 | 3 | 升 targetSdk 即集体失效 |

**最紧急的 5 个问题（建议优先处理）**：
1. `V91MmanAdapter` 漏注册删除监听 → 分分钟本地收藏左滑删除失效（不报错、最难被发现）。
2. `AuthorFragment.loadAuthorVideos()` 绕过 null 守卫 → 登录后查看作者视频路径 NPE 崩溃（用户高频路径）。
3. `HlsDownloadService` 用 `Uri.fromFile` 包通知 → Android 7+ 点"下载完成"必崩。
4. 数据库迁移：`V9MmanItem.isLocalFavorite` 的 `NOT NULL` 无默认列 + 迁移无事务/吞异常 → 旧版升级整表静默清空。
5. 更新链路（个人 GitHub + 零校验 + 无签名/哈希校验 + 公共存储落盘）→ 供应链投毒可装任意 APK。

---

## 二、高严重度问题

### 2.1 崩溃 / NPE（确定性）

| # | 位置 | 问题 |
|---|---|---|
| C1 | `AuthorFragment.java:143-148` ← 调用方 `BasePlayVideo.java:601` | `loadAuthorVideos()` 公开无参重载绕过了同文件已有的 `canLoadAuthorVideos()` 守卫；在"登录成功→查看作者视频"路径上，若视频尚未解析完成，`v9MmanItem.getVideoResult()` 为 null → **确定 NPE 崩溃**。 |
| C2 | `V91MmanAdapter.java:27-34` ← `PornyFavoriteActivity.java:60,70` | `convert()` 未调用 `helper.addOnClickListener(R.id.right_menu_delete)`，导致 `setOnItemChildClickListener` 永不触发 → **分分钟本地收藏左滑删除完全失效**（不崩溃、不提示，仅靠长按删除兜底，最易漏报）。 |
| C3 | `HlsDownloadService.java:129` | 通知 `PendingIntent` 内用 `Uri.fromFile(mp4File)`，`targetSdk≥24` 点击通知时抛 `FileUriExposedException`，**当前设备即崩**。同项目 `FinishedFragment:147`、`UpdateDownloadService:154` 已正确做版本分支，唯独此处漏了。 |
| C4 | `NotificationChannelHelper.java` + `MainActivity.java:108` | 通知渠道只在 `MainActivity` 创建；`UpdateDownloadService`/`DownloadVideoService` 的"通知栏按钮"经 `PendingIntent` 直连 Service，进程被回收后用户点按钮会在无 `MainActivity` 情况下拉起 Service → `startForeground` 抛 `Bad notification for startForeground` 崩溃。`HlsDownloadService` 已在 `onCreate` 自建渠道，是正确范本。 |
| C5 | `SettingPresenter.java:238` 及 `AppCacheUtils.java:98,118,138,188` | 多处 `File.listFiles()` 未判空。默认下载目录不存在（权限被拒/从未创建）时返回 null → 设置页点"自定义下载目录"**NPE 崩溃**。 |
| C6 | `HostJsScope.java:121-133` | `getIMSI()` 权限判断**写反**：未授权时反而去调 `getSubscriberId()`；且 `READ_PHONE_STATE` 全项目无任何运行时申请 → **SecurityException**。该方法经 WebView JS 桥暴露，属未处理崩溃路径。 |
| C7 | `V9MmanItem.java:122` + `SettingActivity.java:314,348` + `SettingPresenter.java:306` | 自定义下载目录存的是"无尾部斜杠"的路径，而消费端直接拼接 `目录+标题+viewKey.mp4` → 文件写到用户所选目录**外面**（如选 `/Movies` 实际写到 `/Movies<标题>...`）。 |
| C8 | `V9MmanItem.java:119-126` | 下载文件名直接使用远端 `getTitle()`，未清洗 `/`(目录穿越)、`../`、超 255 字节、控制字符 → 创建失败或写入异常位置；项目已有 `sanitizeFileName()` 但只用在了 HLS 路径。 |
| C9 | `BasePlayVideo.java:143-146 → 214` | 核心入参 `v9MmanItem` 从 Intent 取出后**全程零校验**，`initData()` 中 `v9MmanItem.getViewKey()` 一旦为 null 即 NPE（同类播放页 `Axgle/KeDou/PlayPxgav` 都写了 `if(xxx==null) return`，唯独此处没有）。 |
| C10 | `CommentFragment.java:137-139` | `onLazyLoadOnce` 两级解引用 `v9MmanItem.getVideoResult().getVideoId()` 无判空，早于视频解析完成时触发 → NPE。`AuthorFragment` 已用 `canLoadAuthorVideos()` 处理同类时序，此处缺对等防护。 |
| C11 | `MineFragment.java:132-141` | `postDelayed(200ms)` 未在 `onDestroyView` 移除、未判 `isAdded()/observableScrollView!=null` → Fragment 销毁后 Runnable 触发 NPE（视角 `VideoListFragment:200` 已正确判空）。 |
| C12 | `AuthorActivity.java:80-83` | 参数非法只 `return` 不 `finish()`，留下半初始化 Activity；配合 mosby ViewState 在旋转时回放 `setData` → null adapter **NPE**。同 `Axgle/KeDou/PlayPxgav` 早退也未 `finish`（白屏，低危）。 |
| C13 | `UserLoginActivity.java:293-330` | OCR 订阅未与 `onDestroy` 的 `captchaOcr.recycle()`/`bitmap.recycle()` 联动；识别中途退出 → 操作已释放 native 句柄或 recycled bitmap → **native 层崩溃**。 |

### 2.2 裸 RxJava 订阅（泄漏 + 销毁后操作 UI）—— 集中在新增收藏功能线

以下均为 Activity 内直接 `Observable.just(1)...subscribe()`，**未 `compose(bindUntilEvent)`、不保存 Disposable**（对比 19 个 Presenter 都已规范绑定生命周期）：

| # | 位置 | 风险 |
|---|---|---|
| R1 | `BasePlayVideo.java:292-311, 321-329, 547-557` | 作者收藏点击 / 收藏态刷新 / 本地收藏，销毁后仍 `updateFavoriteIcon()`、`showMessage()`；`isFinishing()` 在旋转场景不成立，Toast 挂已销毁 Window 可能 `BadTokenException`。 |
| R2 | `AuthorActivity.java:106-114, 123-143` | 初始收藏态读取、切换收藏，同 R1。 |
| R3 | `AuthorFavoriteActivity.java:111-127, 143-154` | `onResume` 反复订阅且从不 dispose；`doDelete` 回调内再开新订阅 → 链式累积。该 Activity 继承 `BaseAppCompatActivity`（无 LifecycleProvider），最严重。 |
| R4 | `PornyFavoriteActivity.java:112-128, 143-156` | 同构，且它本是 `MvpActivity` 手握 provider 却未用。 |
| R5 | `ProxyPresenter.java:48-50` | 唯一漏绑生命周期的 Presenter（`testProxy` 缺 `bindUntilEvent`），网络超时请求残留。 |

> 根因：新增"作者收藏 / 本地收藏"功能把耗时操作写在 Activity 里而非 Presenter，绕开了项目自身的 MVP+rxlifecycle 规范。

### 2.3 数据不可逆丢失

| # | 位置 | 问题 |
|---|---|---|
| D1 | `entity/V9MmanItem.java:44` + `MySQLiteOpenHelper` 迁移 | `isLocalFavorite` 新增为 `INTEGER NOT NULL` 且无默认值。`MigrationHelper.restoreData` 回填 SQL 会因 `NOT NULL constraint failed` 抛异常，被库内 `catch(SQLException)` **静默吞掉且 release 包无日志** → `V9_MMAN_ITEM` 整表（下载记录+观看历史+本地收藏）升级后**静默清空**。 |
| D2 | `MySQLiteOpenHelper.java:34-47` | 迁移全程**无事务**；临时表为连接级 `TEMP TABLE`（进程被杀即消失）。drop 旧表后、回填完成前被杀（结合 D4 主线程 ANR 极易触发）→ 数据永久损毁。 |
| D3 | `AppDbHelper.java:100-116` | `findV9MmanItemByViewKey` 的 `catch(Exception)` 会**删除该 viewKey 全部命中行**。其立论前提（"之前无唯一约束"）已随 `IDX_V9_MMAN_ITEM_VIEW_KEY` 建立而失效，如今任何偶发 SQLite 异常都会触发误删用户数据。 |
| D4 | `AppDbHelper.java:33-42` + `MyApplication.java:38` | 建库 + 全表迁移 + 4 次 `initCategory` 全部在 `Application.onCreate` 主线程同步执行 → 数据量大时**阻塞数秒至 ANR**，放大 D2 损毁窗口。 |
| D5 | `AppDbHelper.java:184-203` + `PlayVideoPresenter.java:229` + `AuthorActivity.java:124` | **作者收藏唯一性完全无保障**：① `AUTHOR_FAVORITE` 表 DB 层零索引/零唯一约束；② `PlayVideoPresenter.addAuthorFavorite` 入库前**无查重**（与 `AuthorPresenter` 不对称）；③ `Schedulers.io()` 上 check-then-act 非原子，快速双击即插重复行；④ 一旦出现重复行，`findAuthorFavorite().unique()` 持续抛 `DaoException` → 该作者收藏/取消**永久损坏且无法自愈**。 |

### 2.4 安全 / 供应链

| # | 位置 | 问题 |
|---|---|---|
| S1 | `AppApiHelper.java:80-83`（更新源）+ `UpdateDownloadService.java:100,113-118` | 更新链路四连缺陷串联成供应链攻击链：① 信任根=个人 GitHub 账号（失陷/用户名被抢注即可推任意包）；② `apkDownloadUrl` 零校验（未强制 https、未校验 host）；③ 模型无 sha256/签名字段，`completed()` 直接 `installApk()` 不做完整性校验；④ APK 落公共 `/sdcard/Download/`，全局可读写，TOCTOU 可被第三方替换。 |
| S2 | `okhttp/TLSSocketFactory.java:33-49` | `DEFAULT_TRUST_MANAGERS.checkServerTrusted` 空实现，信任任何证书。当前装配代码（`ApiServiceModule.java:100`）已注释未启用，属**高危死代码**——调试时取消注释即令全 App（含更新通道）TLS 归零。建议直接删除。 |

---

## 三、中严重度问题

### 3.1 数据层
- **M1** `build.gradle:80`：`schemaVersion 8` 单版本承载三处结构变更（新增 AuthorFavorite 表 + V9MmanItem 两新列），注释只记一处。若 v8 曾以"无 AuthorFavorite"形态发布过，老设备 `user_version=8` 永不触发 `onUpgrade` → `no such table: AUTHOR_FAVORITE` 崩溃。建议本次发布提至 9。
- **M2** `AppDbHelper.java:44-66`：`initCategory` 判重只看"行数是否等于默认数组长度"，默认分类数组一旦调整即成倍插入重复行。
- **M3** `AutoCompleteEntity` 成死实体：`AppDbHelper` 无任何读写方法，搜索联想词写入逻辑在某次重构中丢失（v8 声称保护的"搜索联想词"实际无写入方，属功能回退）。
- **M4** `entity/V9MmanItem.java:109-117`：`equals/hashCode` 用 `id.equals()`，`id==null`（网络解析未入库对象）时 NPE；被大量放入 Adapter List 的任何 `contains/indexOf` 都会崩。
- **M5** `AppDbHelper.java:90-92` + `PlayVideoPresenter.java:200,221`：`insertOrReplace` 在 viewKey 冲突时销毁旧行，`downloadId/status/progress` 等字段被重置为默认值，观看/下载历史丢失。
- **M6** `AppDbHelper.java:28-37`：`IdentityScopeType.Session` 缓存实体跨 IO/主线程裸传递（AuthorFavorite/V9MmanItem 未 `detachAll`，仅 Category 做了），存在可见性/撕裂风险。

### 3.2 网络 / 安全
- **M7** `AndroidManifest.xml:141-143`：Bugsnag API Key 明文硬编码（且 `Bugsnag.init` 已注释，是死配置，建议移除）。
- **M8** `Api.java:10`：西刺代理源站用**明文 http** 抓取，中间人可篡改诱导用户流量导向攻击代理（该站已停服，功能实际失效）。
- **M9** `AppPreferencesHelper.java:105-121` + `AndroidManifest.xml:20`(`allowBackup=true`)：登录密码以 Base64（编码非加密）存储，且 `com.m3man_preferences.xml` 未被 `backup_descriptor.xml` 排除 → `adb backup`/云备份可导出密码。
- **M10** `UpdatePresenter.java:51-52` + `AppApiHelper.java:399`：`gson.fromJson` 返回 null（version.txt 为空/被 CDN 错误页）时直接解引用 → NPE。
- **M11** `res/xml/filepaths.xml:2-4`：`<external-path path="."/>` 将**整张 SD 卡**纳入 FileProvider 可共享范围，越权读取风险。
- **M12** `ApiServiceModule.java:92-120`：更新通道（raw.githubusercontent.com）未做证书固定（Certificate Pinning）。
- **M13** `ApiServiceModule.java:75-84`：`HttpLoggingInterceptor` 无条件 `Level.HEADERS`，release 包泄露含 Cookie 的请求/响应头。
- **M14** `MyProxySelector.java:73-89` + `ProxySettingActivity.java:178-208`：`select()` 返回 null 违反契约（部分栈 NPE）；端口只校验 `>0` 未校验 `<=65535`，`Integer.parseInt` 对超长数字抛 `NumberFormatException`；测试路径有范围校验而保存路径没有，逻辑不一致。
- **M15** `MyProxySelector.java:43-71`：代理测试状态在单例中残留，崩溃/被杀未清理则所有请求（含更新）被强制导向未验证代理；`proxyList` 跨线程并发无同步。
- **M16** `AppApiHelper.java:215-223, 276-280, 545-566, 626-632`：多处 API 响应链式解引用无判空（`getA()` null → NPE；`Integer.parseInt` 遇错误页 → NumberFormatException；`axgle.getResponse()` 可能 null）。
- **M17** `res/xml/network_security_config.xml:1-4`：`<base-config cleartextTrafficPermitted="true"/>` 全局放开明文，使 S1 的 http 降级攻击成立。

### 3.3 存储 / 权限 / 兼容（潜伏，升 targetSdk 即炸）
- **M18** `SDCardUtils.java:11`：下载根目录硬编码 `/sdcard/3mman/`，targetSdk≥29 后全线 `EACCES`；全项目零处用 MediaStore/SAF/`getExternalFilesDir()` 作下载落地。
- **M19** `UpdateDownloadService.java:94`：APK 升级包写入公共 Download 目录，新系统上应用内升级不可用（应改 `getExternalFilesDir`）。
- **M20** `AndroidManifest.xml:8-9`：`WRITE_EXTERNAL_STORAGE` 缺 `android:maxSdkVersion="28"`（API29+ 失效却仍弹窗）；缺 `READ_MEDIA_VIDEO/IMAGES`（API33+）。
- **M21** `exolibrary/.../AndroidManifest.xml:5`（注入主包）：`WRITE_SETTINGS` 特殊权限，但 exolibrary 仅对亮度做 `getInt` 只读，**根本不需要写权限** → 应用商店合规高危项，建议 `tools:node="remove"`。
- **M22** `AndroidManifest.xml:11` `READ_PHONE_STATE`：有声明、有调用（`HostJsScope:130`）但**从不运行时申请**（见 C6）。
- **M23** 缺 `POST_NOTIFICATIONS`（API33+），靠 targetSdk28 兼容，升级后下载通知静默不显示。
- **M24** `DownloadVideoService.java:50-54`：后台 Service 未在 `onStartCommand` 内立即 `startForeground`，Android 8+ 切后台约 1 分钟被系统停止，下载中断。
- **M25** `HlsDownloadService.java:36-101` + `HlsDownloader.java:63-151`：并发下载状态串台、同名覆盖、`downloader` 单字段致旧任务成孤儿线程、共用 `NOTIFICATION_ID=10086` 进度串台、任一任务完成即 `stopSelf` 误摘另一任务前台身份。
- **M26** `HlsDownloader.java:63-66,104-122`：4 线程池 `newFixedThreadPool` 永不 `shutdown()`（泄漏）；分片落 `getCacheDir()`（内部存储，易 ENOSPC，系统低存储时清 cache 致文件消失）；"并发"实为串行。
- **M27** `JiaoZiVideoPlayerActivity.java:33-35`：`thumbImageView.setImageURI(httpUrl)` 不支持网络图 → 封面功能失效 + 潜在主线程同步解码。
- **M28** `AppCacheUtils.java:149-163`：缓存清理复制粘贴错误，`cleanRxCache` 清的是视频缓存目录 → RxCache 永远清不掉；`deleteDirFile` 不递归。
- **M29** `DownloadManager.java:41,170`：`updaterList` 为普通 `ArrayList` 跨线程无同步，`clone()` 期间可能 `ConcurrentModificationException`，应改 `CopyOnWriteArrayList`。
- **M30** `DownloadVideoService.java:38-42`：`onBind` `throw null`（模板残留，当前未触发但语义应为 `return null`）。
- **M31** `AndroidManifest.xml:124-126` Service 缺 `android:foregroundServiceType`（API34 阻断项）；6 处 `PendingIntent` 缺 `FLAG_IMMUTABLE`（API31 阻断项）。

---

## 四、低严重度 / 潜伏（略列要点）
- **L1** `Axgle/KeDou/PlayPxgav` 播放页早退未 `finish()` → 白屏（同 C12，但后续回调少）。
- **L2** `BaseFragment.java:38-43`：`onAttach` 缓存 `activity/context`，`onDetach` 未置空 → 潜在泄漏。
- **L3** `UpdateDownloadService.java:168-173`：`FLAG_GRANT_WRITE_URI_PERMISSION` 对安装场景多余（只需 READ）。
- **L4** `UpdateDownloadService.java:131-139`：下载失败 `error/warn` 空实现，无提示、前台服务不释放。
- **L5** `V9MmanItem.java:122` 文件名未限制长度/控制字符/唯一性。
- **L6** `VideoCacheFileNameGenerator.java:19-28`：地址不含 `.mp4` 时 `substring` 抛异常，catch 返回整 URL 作文件名。
- **L7** `entity/VideoResult.java` `equals/hashCode` 以 `videoId` 判等，与 `@Id` 主键语义不一致；`VIDEO_RESULT` 无唯一索引，`insertOrReplace` 长期冗余增长。
- **L8** `Category.java/V9MmanItem.java/VideoResult.java`：`public Long id` 破坏封装。

---

## 五、全局 grep 旁证
- `TODO` 残留 6 处：`HostJsScope.java:123`、`DownloadVideoService.java:40`、`MainActivity.java:406,423`、`BasePlayVideo.java:180`、`BasePlayVideo.java:180`（其中 C6 即由 HostJsScope 的 TODO 模板未修正导致）。
- 明文 `http://` 命中：`Api.java:10`（西刺，M8）、`Parse91PornyVideo/HlsDownloader` 仅作 URL 解析判断、多处注释/license 链接（无害）。**无业务接口明文硬编码 secret**。
- `Uri.fromFile` 命中 3 处：`HlsDownloadService.java:129`（C3，必崩）、`UpdateDownloadService.java:166`（`<N` 分支合法）、`FinishedFragment.java:151`（`<N` 分支合法）。
- `getExternalStorageDirectory` 命中 2 处：`SDCardUtils.java:11`（M18）、`UpdateDownloadService.java:94`（M19）。

---

## 六、值得肯定的部分（无问题）
- 19/20 个 Presenter 已规范 `compose(provider.bindUntilEvent(...))`；`BaseAppCompatActivity` EventBus 对称注册 + `isFinishing()` 保护；`BaseFragment` 实现 category 保存/恢复。
- APK 安装的 FileProvider 适配正确（Android 7+ 无 `FileUriExposedException`，`canRequestPackageInstalls()` 引导正确，Manifest 声明匹配）。
- 未信任用户 CA 证书（抓包工具无法解密 HTTPS）；未绕过 hostname 校验；未向系统/WebView 注入代理。
- `HlsDownloadService` 在 `onCreate` 自建通知渠道 + 立即 `startForeground`，是唯一符合 Android 8+ 规范的 Service 骨架。
- Glide 全部 `with(itemView)` + placeholder，列表滚动 OOM 风险低；无 static Activity/Context 泄漏；EventBus 与下载监听器注册/注销全部配对；debug 已集成 LeakCanary。

---

## 七、建议修复顺序（供后续参考，本次未改）
1. **确定崩溃**：C2（左滑删除）、C1（作者视频 NPE）、C3（Hls 通知）、C5（设置页 NPE）、C6（getIMSI）、C7/C8（下载路径/文件名）。
2. **数据丢失**：D1/D2/D3（迁移事务+NOT NULL 默认+去除误删）、D5（AuthorFavorite 加 DB 唯一索引 + 入库查重 + 防抖）、M1（schemaVersion 提 9）。
3. **供应链**：S1（version.txt 增加 sha256 校验 + 下载前比对 + 下载路径改私有目录）、S2（删除信任所有证书死代码）、M17（收紧明文）。
4. **架构偿还**：R1-R5（裸订阅收归 Presenter 并绑生命周期）、C9/C10/C11/C12（补齐 null 守卫 + `isFinishing()||isDestroyed()`）。
5. **兼容性预备**（如计划上架/升 targetSdk）：M18-M31 批量迁移到作用域存储 + `getExternalFilesDir` + 补齐前台服务类型/权限/FLAG_IMMUTABLE。

> 备注：上轮已修复的"AuthorFavoriteAdapter 左滑删除失效(P0)"属作者收藏列表，本次发现的是**另一个** V91MmanAdapter（分分钟本地收藏列表）的同类型问题（C2），两者相互独立。
